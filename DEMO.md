# SPC Demonstration — Shared Semantic State in action

> Generated by `spc-demo demo` (deterministic). Re-run it yourself with the command at the bottom.

The Semantic Processing Computer (SPC) coordinates AI stages through a **persistent, versioned semantic state** — typed claims, evidence, assumptions, and contradictions — that every operator changes only by proposing a validated `SemanticPatch`. This walkthrough runs the §8 North Star Demo and contrasts it with a competent JSON-handoff chain.

## 1. The question and the document

**Decision:** Should the company adopt an AI coding assistant?

```text
A company is evaluating whether to adopt an AI coding assistant. The engineering team expects productivity gains, but finance is concerned about usage-based costs. Security has concerns about source code exposure. Prior benchmark studies suggest coding assistants can accelerate routine tasks, but evidence is weaker for complex architecture work.
```

## 2. SPC builds semantic state, one governed patch at a time

Each operator reads a perspective-specific projection, proposes a patch, and the runtime validates -> routes -> commits it into a new state version. No operator mutates state directly.

| step | operator | decision | state | what it wrote |
|---|---|---|---|---|
| 1 | `extract_transform` | COMMIT | v1 | added claim_001, claim_002, claim_003, ev_001, ev_002, ev_003, assumption_001 |
| 2 | `planner_transform` | COMMIT | v2 | added inf_001, hyp_001, q_001, rel_001 |
| 3 | `critic_transform` | COMMIT | v3 | added claim_001, q_002, rel_002; claim_001 confidence 0.74->0.62 |

The result is `SemanticState v3` — a queryable object graph, not a paragraph. Every claim carries its evidence, every confidence change carries its reason, and the whole history is in the audit log.

## 3. The baseline: a competent JSON handoff that keeps no state

The same document through `summary -> critique -> final memo`. It produces a reasonable memo, but each stage re-reads the full document (~87 tokens x 3 = ~261 source tokens reprocessed) and nothing durable survives the handoffs: no ids, no provenance, no assumptions, no transform history.

```json
"recommendation": "Pilot the AI coding assistant on routine engineering tasks, with cost monitoring and source-code controls, before any broad rollout."
```

## 4. The payoff: follow-ups answered from state, not re-reasoned

After both systems answer, we ask the spec §8.4 follow-ups. The SPC engine answers all 8 **directly from committed state** — no model call, no re-reading the document:

- **What did the critic add?**
  - critic_transform added claim_001, q_002, rel_002; changed claim_001 confidence 0.74→0.62.
- **Which claims are weakest?**
  - Weakest claims (low confidence first): claim_001 (0.62), claim_003 (0.83), claim_002 (0.85)
- **Which assumptions most affect the conclusion?**
  - Assumptions by impact: assumption_001 (high) → claim_001
- **Which source supports claim_001?**
  - claim_001 is supported by: ev_001
- **What changed between state v1 and v3?**
  - v1→v3: +6 added, ~1 changed, -0 removed.
- **Which unresolved questions remain?**
  - Unresolved questions: q_001, q_002
- **Which recommendation depends on assumption_001?**
  - Objects depending on assumption_001: claim_001, hyp_001
- **Which claims were inferred rather than observed?**
  - Claims with epistemic status 'inferred': claim_001

The baseline cannot: half of these have no durable object to read at all (no ids, no versions, no provenance), and the rest require re-running the chain.

### The demo moment (spec §8.5)

**What did the critic add?**

- _Baseline:_ “Let me reason through that again.” — The chain kept no record of the critique step's structured effect. Answering means re-reading the critique prose and re-deriving what, if anything, it changed.
- _SPC:_ “Here is the exact object, patch, dependency, and state version where that entered the analysis.” — critic_transform added claim_001, q_002, rel_002; changed claim_001 confidence 0.74→0.62.

## 5. Scorecard (spec §20)

| Metric | SPC vs. baseline |
|---|---|
| §20.1 Semantic Continuity | SPC preserves 5/5 key distinctions as queryable objects; the baseline keeps 3/5 in prose only and 0/5 as anything the system can query. |
| §20.2 Provenance Completeness | SPC: 3/3 major claims carry provenance (100%). Baseline: 0/3 (0%). |
| §20.3 Drift Rate | SPC: 0/1 claim mutations unexplained (0%), all diff-visible. Baseline: 2/2 unexplained (100%) and undetectable by the system itself. |
| §20.4 Reprocessing Burden | SPC reprocesses the source once (~87 tokens); the baseline re-reads the full document 3x (~261 source tokens, ~3.0x), repeating every span. SPC's later operators read derived slices (planner ~71, critic ~54 tokens), not the source. |
| §20.5 Contradiction Detection | SPC preserves 1 tension(s) as queryable, linked objects; the baseline holds 2 only as critique prose (0 queryable). |
| §20.6 Assumption Sensitivity | SPC traces 1/1 assumption(s) to the claims and hypotheses they drive, ranked by impact; the baseline has no assumption objects to traverse. |
| §20.7 State Reuse Efficiency | SPC answers 8/8 follow-ups from state with 0 new source tokens; the baseline answers 0 from durable artifacts and would reprocess ~3632 tokens to attempt them. |
| §20.8 Audit Clarity | SPC exposes 7 kinds of audit artifact (transform log, audit events, versions, diffs, patches, validation, receipt); the baseline exposes 0. |

Full report: `runs/demo/report/pilot_report.md`.

## Run it yourself

```bash
pip install -e ".[dev]"
spc-demo demo
```

Artifacts land under `runs/demo/` — state snapshots, patches, validation reports, diffs, the audit log, the Reasoning Receipt, the baseline transcript, and the full pilot report.
